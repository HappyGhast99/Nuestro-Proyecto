description = "Generate OpenAPI 3.0 documentation for all API endpoints and serve it at GET /docs"

prompt = """
Generate OpenAPI 3.0 documentation for all API endpoints and serve it at GET /docs with an interactive UI.

**Steps**

1. **Verify dependencies**: Ensure swagger-ui-express and swagger-jsdoc are installed in package.json.
2. **Setup configuration**: Check if index.js imports and uses swagger-jsdoc and swagger-ui-express to serve /docs.
3. **Verify API endpoints**: Check if routes are documented with @swagger annotations.
4. **Run and check**: Confirm Swagger UI works on http://localhost:3000/docs.
"""
